export class User {
  username: string;
  dept_id: string;
  firstname: string;
  lastname: string;
  email: string;
  contact: number;
  status?: boolean;
  contact_verified?: boolean;
  email_verified?: boolean;
}
