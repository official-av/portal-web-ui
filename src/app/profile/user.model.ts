export class User {
  username: string;
  dept: string;
  first_name: string;
  last_name: string;
  email: string;
  phonenum: number;
  status_flag?: boolean;
  mobile_flag?: boolean;
  email_flag?: boolean;
}
